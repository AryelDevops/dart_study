main() {

  var nota = 2.3;
  // Não use ; em estruturas de controle; * EXCEÇÃO DO/WHILE!!!;
  // No exemplo abaixo o ; impede que o if possa receber a função print em seu bloco;
  if(nota >= 9.0); {
    print('Parabéns! Vc foi brilhante');
  }
}